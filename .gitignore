# The written document stays private: it contains the live-site audit
# (platform versions, plugin list, hosting) which should not sit on a
# public URL. Send it by email instead.
Cocody-Website-V2-Options.docx
